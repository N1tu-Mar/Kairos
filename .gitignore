# Secrets — gitignored from commit one. The repo is public.
.env
.env.local
*.pem
.aws/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local state
*.db
*.sqlite3
.kairos/
/runs/

# Node / Next.js
node_modules/
web/.next/
web/out/
.vercel/

# OS
.DS_Store

# Raw scraped pages. Regenerable, large, and owned by the sites they came
# from. The audit trail that matters is committed: each page's .meta.json
# (URL, timestamp, status, robots decision, content hash) and the cached
# robots.txt the fetch decision was made against.
data/raw/pages/**/*.html
data/raw/scrape_runs.jsonl

# Next.js frontend build output (source is committed).
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/coverage/
frontend/next-env.d.ts
frontend/tsconfig.tsbuildinfo

# docs

.session
session.md
